# cc-session-viewer

A CLI tool to browse and visualize Claude Code session logs in your browser.

Built with Deno. Reads `~/.claude/projects` session files (read-only) and serves a local web UI.

## Usage

```
deno run -A jsr:@kt3k/cc-session-viewer
```
